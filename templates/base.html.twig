<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}FootMatch - Vente de maillots de football{% endblock %}</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            display: flex;
            flex-direction: column;
            min-height: 100vh;
            background-color: #fafafa;
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
            color: #2d2d2d;
            line-height: 1.6;
        }

        main {
            flex: 1;
            padding: 40px 0;
        }

        nav {
            background: #fff;
            border-bottom: 1px solid #e5e5e5;
            box-shadow: none;
            padding: 16px 0;
        }

        .navbar-brand {
            font-weight: 600;
            font-size: 1.3rem;
            color: #2d2d2d !important;
            letter-spacing: -0.5px;
        }

        .nav-link {
            color: #666 !important;
            font-weight: 500;
            font-size: 0.95rem;
            transition: color 0.2s ease;
            margin: 0 8px;
        }

        .nav-link:hover {
            color: #2d2d2d !important;
        }

        .navbar-toggler {
            border: none;
            padding: 0;
        }

        .navbar-toggler:focus {
            box-shadow: none;
            outline: 1px solid #d0d0d0;
            border-radius: 4px;
        }

        .dropdown-menu {
            border: 1px solid #e5e5e5;
            box-shadow: 0 8px 24px rgba(0,0,0,0.08);
            border-radius: 8px;
        }

        .dropdown-item {
            color: #666;
            font-weight: 500;
            transition: all 0.2s ease;
        }

        .dropdown-item:hover {
            background-color: #f5f5f5;
            color: #2d2d2d;
        }

        .dropdown-divider {
            border-color: #e5e5e5;
        }

        footer {
            background-color: #fff;
            color: #666;
            padding: 60px 0 40px;
            margin-top: 80px;
            border-top: 1px solid #e5e5e5;
            font-size: 0.95rem;
        }

        footer a {
            color: #666;
            text-decoration: none;
            transition: color 0.2s ease;
        }

        footer a:hover {
            color: #2d2d2d;
        }

        .container-fluid {
            max-width: 1200px;
            margin: 0 auto;
            padding-left: 20px;
            padding-right: 20px;
        }

        .alert {
            border: 1px solid #e5e5e5;
            border-radius: 6px;
            margin: 20px auto;
            max-width: 1200px;
            font-weight: 500;
        }

        .alert-danger {
            background-color: #fef2f2;
            color: #7f1d1d;
            border-color: #fecaca;
        }

        .alert-success {
            background-color: #f0fdf4;
            color: #15803d;
            border-color: #bbf7d0;
        }

        .alert-warning {
            background-color: #fffbeb;
            color: #92400e;
            border-color: #fde68a;
        }

        .alert-info {
            background-color: #f0f9ff;
            color: #0c4a6e;
            border-color: #bae6fd;
        }

        .btn {
            border-radius: 6px;
            font-weight: 500;
            padding: 10px 20px;
            font-size: 0.95rem;
            transition: all 0.2s ease;
            border: none;
        }

        .btn-primary {
            background-color: #2d2d2d;
            color: #fff;
        }

        .btn-primary:hover {
            background-color: #1a1a1a;
            color: #fff;
        }

        .btn-secondary {
            background-color: #e5e5e5;
            color: #2d2d2d;
        }

        .btn-secondary:hover {
            background-color: #d0d0d0;
            color: #2d2d2d;
        }

        .btn-outline-primary {
            border: 2px solid #2d2d2d;
            color: #2d2d2d;
        }

        .btn-outline-primary:hover {
            background-color: #2d2d2d;
            color: #fff;
        }

        .form-control, .form-select, textarea {
            border: 1px solid #d0d0d0;
            border-radius: 6px;
            padding: 10px 14px;
            font-size: 0.95rem;
            font-family: inherit;
            transition: all 0.2s ease;
        }

        .form-control:focus, .form-select:focus, textarea:focus {
            border-color: #2d2d2d;
            box-shadow: 0 0 0 3px rgba(45,45,45,0.08);
            outline: none;
        }

        .form-label {
            font-weight: 500;
            color: #2d2d2d;
            margin-bottom: 8px;
        }

        .section {
            padding: 40px 0;
        }

        .section-title {
            margin-bottom: 40px;
        }

        .grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 40px;
        }

        .grid-2 {
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
        }

        .grid-item {
            display: flex;
            flex-direction: column;
        }

        .item-title {
            font-weight: 600;
            color: #2d2d2d;
            margin-bottom: 12px;
        }

        .item-subtitle {
            color: #999;
            font-size: 0.95rem;
            margin-bottom: 8px;
        }

        .item-description {
            color: #666;
            margin-bottom: 16px;
            line-height: 1.6;
        }

        .item-footer {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-top: 16px;
        }

        .item-price {
            font-weight: 600;
            color: #2d2d2d;
        }

        .item-stock {
            font-size: 0.85rem;
            color: #999;
        }

        .space-y-3 {
            margin-bottom: 24px;
        }

        .space-y-4 {
            margin-bottom: 40px;
        }

        .space-y-5 {
            margin-bottom: 60px;
        }

        .space-t-2 {
            margin-top: 16px;
        }

        .text-gray {
            color: #666;
        }

        .text-light-gray {
            color: #999;
        }

        .text-small {
            font-size: 0.85rem;
        }

        .text-center-content {
            text-align: center;
        }

        .rounded-full {
            border-radius: 50%;
        }

        .flex-between {
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .flex-col {
            display: flex;
            flex-direction: column;
        }

        .flex-center {
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .button-primary {
            background-color: #2d2d2d;
            color: #fff;
            padding: 10px 20px;
            border-radius: 6px;
            font-weight: 500;
            display: inline-block;
            transition: all 0.2s ease;
            border: none;
            cursor: pointer;
            font-size: 0.95rem;
        }

        .button-primary:hover {
            background-color: #1a1a1a;
            color: #fff;
            text-decoration: none;
        }

        .button-secondary {
            background-color: #e5e5e5;
            color: #2d2d2d;
            padding: 10px 20px;
            border-radius: 6px;
            font-weight: 500;
            display: inline-block;
            transition: all 0.2s ease;
            border: none;
            cursor: pointer;
            font-size: 0.95rem;
        }

        .button-secondary:hover {
            background-color: #d0d0d0;
            color: #2d2d2d;
            text-decoration: none;
        }

        .button-small {
            padding: 6px 12px;
            font-size: 0.85rem;
        }

        .button-large {
            padding: 14px 28px;
            font-size: 1rem;
        }

        .link-item {
            color: #2d2d2d;
            text-decoration: none;
            display: block;
            padding: 8px 0;
            transition: color 0.2s ease;
        }

        .link-item:hover {
            color: #666;
        }

        .link-danger {
            color: #d32f2f;
        }

        .link-danger:hover {
            color: #b71c1c;
        }

        .profile-avatar {
            width: 150px;
            height: 150px;
            object-fit: cover;
        }

        .profile-section {
            text-align: center;
            margin-bottom: 40px;
        }

        .profile-info {
            margin-bottom: 8px;
        }

        .profile-meta {
            font-size: 0.85rem;
            color: #999;
            display: block;
            margin-top: 12px;
        }

        .actions-list {
            display: flex;
            flex-direction: column;
            gap: 8px;
        }

        h1, h2, h3, h4, h5, h6 {
            font-weight: 600;
            color: #2d2d2d;
            margin-bottom: 16px;
        }

        h1 {
            font-size: 2.2rem;
            letter-spacing: -1px;
        }

        h2 {
            font-size: 1.8rem;
        }

        h3 {
            font-size: 1.4rem;
        }

        a {
            color: #2d2d2d;
            text-decoration: none;
            transition: color 0.2s ease;
        }

        a:hover {
            color: #666;
        }

        .text-muted {
            color: #999;
        }

        .small {
            font-size: 0.875rem;
            color: #999;
        }

        .badge {
            background-color: #e5e5e5;
            color: #2d2d2d;
            font-weight: 500;
            padding: 4px 10px;
            border-radius: 4px;
        }

        .table {
            border-collapse: collapse;
        }

        .table thead {
            background-color: #f9f9f9;
            border-bottom: 2px solid #e5e5e5;
        }

        .table thead th {
            font-weight: 600;
            color: #2d2d2d;
            padding: 12px;
            border: none;
        }

        .table tbody td {
            padding: 12px;
            border-bottom: 1px solid #e5e5e5;
        }

        .table tbody tr:hover {
            background-color: #f9f9f9;
        }

        input[type="checkbox"], input[type="radio"] {
            accent-color: #2d2d2d;
        }

        @media (max-width: 768px) {
            .container-fluid {
                padding-left: 16px;
                padding-right: 16px;
            }

            main {
                padding: 20px 0;
            }

            h1 {
                font-size: 1.8rem;
            }

            h2 {
                font-size: 1.4rem;
            }

            .nav-link {
                margin: 0 4px;
                font-size: 0.9rem;
            }
        }
    </style>
    {% block stylesheets %}{% endblock %}
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-light bg-white">
        <div class="container-fluid">
            <a class="navbar-brand" href="{{ path('app_home') }}">
                FootMatch
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_home') }}">Accueil</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_maillots') }}">Maillots</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_contact') }}">Contact</a>
                    </li>
                    {% if app.user %}
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle" href="#" id="userDropdown" role="button" data-bs-toggle="dropdown">
                                <i class="fas fa-user"></i> {{ app.user.fullName }}
                            </a>
                            <ul class="dropdown-menu dropdown-menu-end">
                                <li><a class="dropdown-item" href="{{ path('app_profile') }}">Mon profil</a></li>
                                <li><a class="dropdown-item" href="{{ path('app_change_password') }}">Changer le mot de passe</a></li>
                                <li><hr class="dropdown-divider"></li>
                                <li><a class="dropdown-item" href="{{ path('app_logout') }}">Déconnexion</a></li>
                            </ul>
                        </li>
                    {% else %}
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_login') }}">Connexion</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_register') }}">Inscription</a>
                        </li>
                    {% endif %}
                </ul>
            </div>
        </div>
    </nav>

    <main class="container-fluid">
        {% if app.session.flashBag.peekAll %}
            {% for type, messages in app.session.flashBag.all %}
                {% for message in messages %}
                    <div class="alert alert-{{ type == 'error' ? 'danger' : (type == 'succes' ? 'success' : type) }} alert-dismissible fade show" role="alert">
                        {{ message }}
                        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                    </div>
                {% endfor %}
            {% endfor %}
        {% endif %}

        {% block body %}{% endblock %}
    </main>

    <footer>
        <div class="container">
            <div class="row">
                <div class="col-md-3">
                    <h5>FootMatch</h5>
                    <p>Votre boutique de maillots de football officiels.</p>
                </div>
                <div class="col-md-3">
                    <h5>Navigation</h5>
                    <ul class="list-unstyled">
                        <li><a href="{{ path('app_home') }}">Accueil</a></li>
                        <li><a href="{{ path('app_maillots') }}">Maillots</a></li>
                        <li><a href="{{ path('app_contact') }}">Contact</a></li>
                    </ul>
                </div>
                <div class="col-md-3">
                    <h5>Légal</h5>
                    <ul class="list-unstyled">
                        <li><a href="{{ path('mentions_legales') }}">Mentions légales</a></li>
                        <li><a href="{{ path('cgu') }}">CGU</a></li>
                        <li><a href="{{ path('politique_confidentialite') }}">Politique de confidentialité</a></li>
                    </ul>
                </div>
                <div class="col-md-3">
                    <h5>À propos</h5>
                    <ul class="list-unstyled">
                        <li><a href="{{ path('qui_sommes_nous') }}">Qui sommes-nous</a></li>
                        <li><a href="{{ path('app_contact') }}">Nous contacter</a></li>
                    </ul>
                </div>
            </div>
            <hr>
            <div class="text-center">
                <p>&copy; 2026 FootMatch. Tous droits réservés.</p>
            </div>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    {% block javascripts %}{% endblock %}
</body>
</html>
